migration:
  options:
    install_command: odoo
    install_args: --log-level=debug

  versions:
    - version: setup
      operations:
        # pre:
        #   - psql -c "CREATE EXTENSION IF NOT EXISTS pg_trgm;"
        post:
          - click-odoo songs/setup/base.py
          - /bin/sh -c "VERSION=19.0.1.0.0 click-odoo songs/env.py"
      addons:
        upgrade:
          - customer
          # - monitoring_status
          - web_environment_ribbon
#      modes:
#        external:
#          operations:
#          addons:
#            upgrade:
#              - session_redis
