migration:
  options:
    install_command: odoo
    install_args: --log-level=debug

  versions:
    - version: setup
      operations:
        post:
          - /bin/sh -c "VERSION=19.0.0.0.0 anthem songs.env::main"
      addons:
        upgrade:
          - contacts
